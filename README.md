✔ User Signup

Custom signup form (simple Form + Django User model)

Validations for username, email, password, confirm password

Saves new users using User.objects.create_user()

✔ User Login

Custom login form

Authenticates user credentials

Creates user session using login(request, user)

Routing + Views

Handling POST vs GET

Redirects after login/signup

🔹 Templates

Rendering dynamic data

Using {% url %} tags

Basic form layout & styling
